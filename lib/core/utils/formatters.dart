// Placeholder for future text/number formatters.
class Formatters {
  static String km(double km) => '${km.toStringAsFixed(1)} km';
  static String min(int min) => '$min min';
  static String currency(num n) => 'PKR ${n.toStringAsFixed(0)}';
}
